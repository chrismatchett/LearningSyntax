using System;
public class PerformingOperations
{
  public static void output()
  {
          int x = 100 + 50;
          x++; // 151
          x += 1; // 152
          int y = x % 2;
          //Console.WriteLine(x + ", " + y + ", " + (x == y));

  }
}